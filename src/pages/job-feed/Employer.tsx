import { FC } from "react";
import { EmployerSection, EmployerLayout } from "components";

const Employer: FC = () => {
  return (
    <EmployerLayout>
      <EmployerSection />
    </EmployerLayout>
  );
};

export { Employer };
